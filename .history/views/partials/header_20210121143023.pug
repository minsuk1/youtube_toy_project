
header.header
    .header__column
        a(href=routes.home)
            i.fab.fa-youtube
    .header__column
        form(action='get')
            input(type="text", placeholder="search by term")
    .header__column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Log In